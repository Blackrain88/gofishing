# 바다가자 — 1단계 (PWA 본체)

워킹낚시 · 해루질 포인트와 출조 기록을 관리하는 개인용 앱. 이 단계는 **로컬 저장까지**만 들어 있습니다.

## 들어 있는 것

| 파일 | 설명 |
|---|---|
| `index.html` | 앱 전체 (화면 · 로직 · 스타일) |
| `manifest.json` | PWA 설치 정보 |
| `sw.js` | 서비스워커 — 오프라인 캐시 |
| `icon-192.png` / `icon-512.png` / `icon-maskable-512.png` | 앱 아이콘 |

## GitHub Pages 올리기

1. GitHub에서 새 저장소 생성 (예: `badagaja`) — **Public**
2. 이 폴더의 **6개 파일을 저장소 최상위에** 업로드 (Add file → Upload files → Commit)
3. 저장소 → **Settings → Pages**
   - Source: `Deploy from a branch`
   - Branch: `main` / `/ (root)` → Save
4. 1~2분 뒤 `https://<아이디>.github.io/badagaja/` 로 접속

> Private 저장소는 Pages가 유료 플랜에서만 됩니다. Public으로 두되, 앱에는 아무 데이터도 들어있지 않으니 (데이터는 각자 폰에만 저장됨) 공개해도 문제없습니다.

## 폰에 설치하기

1. 안드로이드 **Chrome**으로 위 주소 접속
2. 우측 상단 ⋮ → **홈 화면에 추가** (또는 자동으로 뜨는 설치 배너)
3. 홈 화면 아이콘으로 실행하면 주소창 없는 앱처럼 동작

설치 후에는 **비행기 모드에서도 그대로 동작**합니다.

## 앱을 수정한 뒤에

`sw.js` 안의 `VERSION = 'badagaja-v1'` 숫자를 올려 주세요(`v2`, `v3`…).
안 올리면 기기에 캐시된 옛날 버전이 계속 뜹니다.

## 이번 단계에서 되는 것

- 포인트 추가 / 수정 / 삭제 (워킹낚시 · 해루질 타입 구분)
- 시/도 › 구/군 › 장소 3뎁스 콤보 — 입력하면 기존 항목이 걸러지고, 없으면 그대로 새로 등록
- 조과물 프리셋 + 직접 입력 (직접 입력한 항목은 프리셋에 자동 축적)
- 시즌 메모 (월별 · 기간별)
- 좌표 저장 — 현재 위치 / 붙여넣기 / 직접 입력
- 포인트 입력 중 **지도앱 바로 열기** — 좌표가 있으면 그 지점을, 없으면 입력한 장소명으로 검색
- 외부 지도앱 길찾기 (네이버 · 카카오 · 구글)
- 출조 기록 추가 / 수정 / 삭제 — 날짜, 조업 시작·종료 시각, 소요시간 자동 계산, 조과물 수량, 메모
- 메인 리스트 ⇄ 썸네일 그리드, 타입 필터, 통합 검색, 정렬
- 리스트·그리드에 **조과물 표시** — 포인트에 적어둔 것 우선, 없으면 출조 기록에서 집계 (많으면 `+N`)
- 포인트별 누적 조과 집계
- JSON 내보내기 / 가져오기

## 다음 단계

2. 사진 · 지도 이미지 첨부, S펜 드로잉, 공유 시트로 이미지 받기
3. 실사용 테스트
4. Google Sheets + Apps Script 동기화
5. 길찾기 실단말 검증
6. Capacitor 포장 → APK

## 알아둘 것

- 데이터는 브라우저의 IndexedDB에 저장됩니다. **크롬 사이트 데이터를 지우면 함께 사라집니다.**
  4단계(클라우드 동기화)를 붙이기 전까지는 설정 → JSON 내보내기로 가끔 백업해 두세요.
- 시크릿 모드에서는 저장이 동작하지 않습니다.
- 네이버·카카오 지도 길찾기는 PWA 상태에서 동작이 불확실합니다. 안 되면 구글지도로 쓰다가 APK 단계에서 다시 확인하면 됩니다.
